import React, { useState } from 'react';


const App = () => {
  const initialStates = {
    name: '',
    price: 1000,
  }
  const[name, setName] = useState(initialStates.name);

  return (
    <React.Fragment>
  
    </React.Fragment>
  );
}

export default App;
